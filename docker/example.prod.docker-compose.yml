sfmdb:
    image: gwul/sfm-ui-db
    environment:
        - POSTGRES_PASSWORD=gherD42#dl5
sfmapp:
    image: gwul/sfm-ui:latest
    ports:
        - "80:80"
    links:
        - sfmdb:db
    environment:
        - SFM_DEBUG=True
    volumes:
        - /sfm-data:/var/sfm
    restart: always
