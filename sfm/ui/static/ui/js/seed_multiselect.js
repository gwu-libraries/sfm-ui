$(document).ready(function() {
        // seed multiselect
        $('#id_seeds').multiselect({
           includeSelectAllOption: true,
           enableFiltering:true,
           maxHeight: 350,
           buttonWidth: 200

        });
 });